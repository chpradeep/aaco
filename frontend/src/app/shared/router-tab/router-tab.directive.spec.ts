import { RouterTabDirective } from './router-tab.directive';

describe('RouterTabDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterTabDirective();
    expect(directive).toBeTruthy();
  });
});
