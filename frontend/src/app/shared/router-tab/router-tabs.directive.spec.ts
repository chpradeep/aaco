import { RouterTabsDirective } from './router-tabs.directive';

describe('RouterTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterTabsDirective();
    expect(directive).toBeTruthy();
  });
});
